# {需求标题} 代码变更记录

> 关联方案：`.comate/specs/{feature_name}/doc.md`
> 关联任务：`.comate/specs/{feature_name}/tasks.md`
> 生成时间：{时间戳}

## 变更概览

| 变更类型 | 文件数 | 新增行数 | 删除行数 |
|---------|-------|---------|---------|
| 新增文件 | {N}   | {N}     | 0       |
| 修改文件 | {N}   | {N}     | {N}     |
| 删除文件 | {N}   | 0       | {N}     |

## 新增文件

| 文件路径 | 关联任务 | 说明 |
|---------|---------|------|
| `{path}` | Task: {任务描述} | {说明} |

## 修改文件

| 文件路径 | 关联任务 | 变更摘要 |
|---------|---------|---------|
| `{path}` | Task: {任务描述} | {变更摘要：如"新增 CreateXxx 方法，修改 GetXxx 返回值"} |

## 删除文件

| 文件路径 | 关联任务 | 删除原因 |
|---------|---------|---------|
| `{path}` | Task: {任务描述} | {原因} |

> 如需查看完整 diff，可在项目根目录执行 `git diff` 获取。
