# {需求标题} 代码实现任务

> 基于方案文档：`.comate/specs/{feature_name}/doc.md`
> 代码探索结论：见 doc.md §6.1

---

按实际需求拆分任务，每个分组内的任务数量不固定。所有 `{路径}` 占位符必须替换为 Step 2 探索到的实际文件路径。

## 数据层变更

- [ ] 执行 §6.2 数据库 DDL
    - 目标：`{实际数据库/表}`
    - 变更：{新建表/修改表结构}

- [ ] 实现 §6.2 数据访问逻辑
    - 目标模块：`{实际数据访问模块路径}`
    - 变更：{新增/修改数据操作方法}

- [ ] 实现 §6.2 缓存逻辑（如涉及，否则删除本条）
    - 目标模块：`{实际缓存模块路径}`
    - Key 定义：见 doc.md §6.2.2

## 业务逻辑层变更

- [ ] 实现 §6.1 核心业务逻辑 — {具体功能描述}
    - 目标模块：`{实际业务逻辑模块路径}`
    - 变更：{新增/修改业务方法}
    - 关键函数：`{函数名}`

（按 doc.md §6.1 架构图中的模块拆分，一个模块一个任务）

## 接口层变更

- [ ] 实现 §6.4 接口 — {接口中文名}
    - 目标模块：`{实际接口模块路径}`
    - 接口定义：见 doc.md §6.4

（按 doc.md §6.4 中的接口拆分，多个简单接口可合并为一个任务）

## 非功能性变更（按需，不涉及的分组直接删除）

- [ ] 实现 §6.5 监控埋点
    - 监控指标：见 doc.md §6.5.2
    - 目标文件：`{路径}`

- [ ] 实现 §6.5 日志规范
    - 日志格式：见 doc.md §6.5.2
    - 目标文件：`{路径}`

## 测试与验证

- [ ] 编写单元测试
    - 覆盖模块：{列出需要测试的模块}
    - 测试文件：`{路径}`

- [ ] 验证功能正确性 ⚠️ 人工确认
    - 验证清单：见 doc.md §8 上线 checklist

## 配置与部署（按需，不涉及则删除）

- [ ] 更新配置文件
    - 配置项：{列出新增/修改的配置}
    - 文件路径：`{路径}`
